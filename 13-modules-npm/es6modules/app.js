import {
  apiKey as key,
  url,
  sayHi,
  age,
  garfield
} from './src/config';

console.log('Yoohoo');
console.log(key);

sayHi('Alice');

console.log(url);
console.log(`${garfield} is ${age} years old`);
